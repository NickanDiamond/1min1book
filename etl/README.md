# ETL

Pipeline: 1Min1Book (read-only) -> extract -> clean -> theme-tag -> weight -> load into `DATABASE_URL`.

Not filled in yet — extract.py needs the spreadsheet ID and auth method
confirmed first (see docs/architecture.md, "open question").
